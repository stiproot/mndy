import type { McpServer } from "mcp-core";

export function registerTools(server: McpServer): void {
  // TODO: Register tools here
  // Use @mcp-tool skill to generate tools:
  // - markdown_generate_file: Generate markdown from structured data
  // - markdown_from_template: Generate from templates
  // - markdown_convert_json: Convert JSON to markdown
  // - markdown_convert_csv: Convert CSV to markdown table
  // - markdown_validate: Validate markdown syntax
  //
  // Example:
  // registerMarkdownGenerateFileTool(server);
  // registerMarkdownFromTemplateTool(server);
  // registerMarkdownConvertJsonTool(server);
  // registerMarkdownConvertCsvTool(server);
  // registerMarkdownValidateTool(server);
}
