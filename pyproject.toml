[project]
name = "mndy"
version = "0.1.0"
description = "Project analytics platform for Azure DevOps"
requires-python = ">=3.12"

[tool.uv.workspace]
members = [
    "packages/py/mndy-framework",
    "apps/azdo-worker",
    "apps/insights-worker",
    "apps/azdoproxy-worker",
    "apps/workflows-worker",
]
