---
paths:
  - "src/**/*.py"
---

# Python Conventions

- Use FastAPI with async patterns
- Import shared types from mndy-framework
- Follow Dapr SDK patterns for service communication
- Linting: uv (see pyproject.toml in each worker)
- Run `uv run ruff check` before committing
