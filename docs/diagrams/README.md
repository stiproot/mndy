# Diagrams

Generated diagrams (via `gen-code-diagram`, the `code-comprehension` plugin) and authored
C4 views (via `c4-mermaid-plugin`) live here. See the Diagrams section of `CLAUDE.md`.

Generated diagrams are checked, not hand-edited:

```bash
gen-code-diagram --check --dir docs/diagrams
```
