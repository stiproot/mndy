# Effect-TS migration: ui-api

Status: Active — migrate the API gateway onto Effect. Currently the only package that
fails `turbo build` (Effect type errors in `src/handlers/chat.handlers.ts`).
Established: 2026-08-06
